/// <reference types="react" />
import { ValueEditorProps } from '../types';
declare const ValueEditor: {
    ({ operator, value, handleOnChange, title, className, type, inputType, values, fieldData }: ValueEditorProps): JSX.Element | null;
    displayName: string;
};
export default ValueEditor;

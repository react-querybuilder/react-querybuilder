/// <reference types="react" />
import { RuleGroupProps } from './types';
export declare const RuleGroup: {
    ({ id, parentId, combinator, rules, translations, schema, not, context }: RuleGroupProps): JSX.Element;
    displayName: string;
};
